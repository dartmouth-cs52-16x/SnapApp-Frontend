import React from 'react';

const Friend = (props) => {
  return (
    <div className="single-friend-full">
      <p>Name: {this.props.name}</p><p>Score: {this.props.score}</p>
    </div>
  );
};

export default Friend;
